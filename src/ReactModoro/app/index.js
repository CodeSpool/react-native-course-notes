import { StackNavigator } from 'react-navigation'
import { SplashContainer } from '~/containers'

const ReactModoro = StackNavigator({
  Home: { screen: SplashContainer }
})

export default ReactModoro
